Веб-приложение "Расписание Лицея"

Это веб-приложение на Flask для отображения школьного расписания в режиме реального времени. Оно автоматически скачивает актуальный XLSX-файл с Яндекс.Диска, парсит его и отображает в стильном адаптивном веб-интерфейсе.
✨ Основные возможности

    Автоматическое обновление: Скачивает расписание с Яндекс.Диска и кэширует его (по умолчанию на 15 минут).

    Адаптивный интерфейс: Отображает расписание по-разному на горизонтальных (таблицы по параллелям) и вертикальных (интерактивные списки классов) экранах.

    "Живое" табло: Показывает расписание только для тех классов, занятия которых скоро начнутся или недавно закончились, создавая эффект актуального информационного стенда.

    Гибкая настройка: Все ключевые параметры (токены, пути, временные интервалы) настраиваются через .env файл.

    Надежность: Умеет обрабатывать сетевые ошибки и проблемы с форматом файла, используя резервные механизмы и подробное логирование.

    Стильный дизайн: Имеет современную темную тему с градиентом и акцентными цветами.

🚀 Установка и запуск

    Клонируйте репозиторий:

    git clone <URL вашего репозитория>
    cd <название папки>

    Создайте и активируйте виртуальное окружение:

    python -m venv venv
    source venv/bin/activate  # Для Linux/macOS
    # venv\Scripts\activate    # Для Windows

    Установите зависимости:

    pip install -r requirements.txt

    Настройте переменные окружения:
    Создайте в корне проекта файл .env и заполните его по следующему шаблону:

    # .env - Файл для хранения переменных окружения

    # Обязательно: Ваш OAuth-токен от Яндекса
    YANDEX_TOKEN="y0_AgAAAAA...ВАШ_ТОКЕН...AAAAA"

    # Обязательно: Путь к файлу на Яндекс.Диске
    YANDEX_FILE_PATH="/путь/к/вашему/schedule.xlsx"

    # Остальные параметры (можно не указывать, будут использованы значения по умолчанию из config.py)
    LOCAL_FILE_PATH="data/schedule.xlsx"
    CACHE_DURATION=900
    CAROUSEL_INTERVAL=30
    SHOW_BEFORE_START_MIN=120
    SHOW_AFTER_END_MIN=30
    REGION_TIMEDELTA=11 # Смещение для Южно-Сахалинска (GMT+11)
    LOGO_FILE_PATH="img/logo.png"

    Запустите приложение:

    flask run
    # или
    python run.py

    Откройте в браузере http://127.0.0.1:5000.