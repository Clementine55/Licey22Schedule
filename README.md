Конечно. Ты проделал титаническую работу. Вот полный, подробный и профессиональный `README.md`, который отражает всю мощь и продуманность твоей новой архитектуры.

Заливай с гордостью. И до завтра!

---

# Licey22Schedule

Веб-приложение для отображения школьных расписаний (уроков и консультаций) на информационных панелях и мобильных устройствах. Приложение динамически загружает и кэширует `.xlsx` файлы с Яндекс.Диска, обеспечивая высокую производительность и отказоустойчивость.

## 🚀 Ключевые особенности

*   **Динамическая загрузка:** Автоматически скачивает и обновляет файлы расписаний с Яндекс.Диска.
*   **Надежный парсинг Excel:** Способность обрабатывать сложные `.xlsx` файлы с различными форматами заголовков и структурами листов.
*   **Продвинутое кэширование:** Все распарсенные данные кэшируются для мгновенной отдачи, минимизируя нагрузку и время отклика. Кэш автоматически обновляется по истечении заданного интервала.
*   **Адаптивный интерфейс:** Два полностью независимых режима отображения:
    *   **Landscape View:** Оптимизирован для информационных панелей и мониторов, отображает несколько классов одновременно в формате карусели.
    *   **Portrait View:** Оптимизирован для мобильных устройств, позволяет пользователю выбрать свой класс и просматривать расписание на всю неделю.
*   **Поддержка нескольких расписаний:** Система конфигурации позволяет легко добавлять и управлять несколькими независимыми файлами расписаний (например, для разных корпусов).
*   **Обработка особого времени:** Корректно обрабатывает "короткие" учебные дни, автоматически подгружая соответствующую сетку звонков.
*   **Интерактивная подсветка:** В реальном времени подсвечивает текущие и следующие уроки/консультации.

## 🛠️ Стек технологий

#### Backend
*   **Python 3.12+**
*   **Flask:** Микрофреймворк для веб-приложения и API.
*   **Gunicorn:** WSGI-сервер для production-развертывания.
*   **Pandas:** Основной инструмент для парсинга `.xlsx` файлов.
*   **python-calamine:** Высокопроизводительный движок для чтения Excel, используемый Pandas.
*   **Yandex.Disk:** Клиент для взаимодействия с API Яндекс.Диска.
*   **python-dotenv:** Управление конфигурацией через переменные окружения.

#### Frontend
*   **HTML5 / CSS3**
*   **JavaScript (ES6+):** Клиентская логика, взаимодействие с API, динамическое обновление.
*   **Bootstrap 5:** CSS-фреймворк для адаптивной верстки и стилизации.

## 📂 Структура проекта

Проект имеет модульную и четко разделенную структуру для легкой поддержки и масштабирования.

```
Licey22Schedule/
├── data/                   # Папка для хранения данных (скачанные файлы, кэш)
├── logs/                   # Папка для лог-файлов
├── app/                    # Основная папка приложения
│   ├── __init__.py         # Инициализация Flask приложения
│   ├── api_routes.py       # Маршруты для API
│   ├── routes.py           # Основные маршруты для HTML-страниц
│   ├── utils.py            # Общие утилиты (JSON сериализатор)
│   │
│   ├── services/           # Пакет с бизнес-логикой
│   │   ├── clients/        # ----- Взаимодействие с внешними сервисами
│   │   │   ├── time_service.py
│   │   │   └── yandex_disk_client.py
│   │   ├── core/           # ----- Ядро бизнес-логики
│   │   │   ├── cache_manager.py
│   │   │   └── view_filter.py
│   │   ├── parsers/        # ----- Модули для парсинга Excel
│   │   │   ├── _landscape_builder.py
│   │   │   ├── _portrait_builder.py
│   │   │   ├── common_structs.py
│   │   │   ├── consultation_parser.py
│   │   │   ├── schedule_parser.py
│   │   │   └── short_day_parser.py
│   │   └── utils/          # ----- Вспомогательные утилиты для сервисов
│   │       ├── bell_schedule.py
│   │       ├── data_validator.py
│   │       └── schedule_verification.py
│   │
│   ├── static/             # Статические файлы (CSS, JS, изображения)
│   └── templates/          # HTML-шаблоны
│
├── .env                    # Файл для секретных переменных окружения (локально)
├── requirements.txt        # Список Python-библиотек
├── run.py                  # Файл для запуска приложения
└── struct.txt              # Описание структуры проекта
```

## ⚙️ Установка и запуск

#### 1. Клонирование репозитория
```bash
git clone https://github.com/your-username/Licey22Schedule.git
cd Licey22Schedule
```

#### 2. Создание и активация виртуального окружения
*   **Linux/macOS:**
    ```bash
    python3 -m venv .venv
    source .venv/bin/activate
    ```
*   **Windows:**
    ```bash
    python -m venv .venv
    .venv\Scripts\activate
    ```

#### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

#### 4. Настройка конфигурации
Создайте файл `.env` в корневой директории проекта, скопировав в него содержимое из `env.example` (если он есть) или создав с нуля. Заполните все необходимые переменные.

**Пример `.env` файла:**
```env
# Обязательно: OAuth-токен для API Яндекс.Диска
YANDEX_TOKEN="y0_AgAAAA..."

# --- Конфигурация расписаний ---
# Можно добавлять YANDEX_FILE_PATH_2, FILE_NAME_2 и т.д. для нескольких файлов

# Расписание №1
YANDEX_FILE_PATH_1="/Путь/К/файлу/на/Яндекс.Диске/schedule1.xlsx"
FILE_NAME_1="main_schedule" # Уникальное имя для URL и файлов кэша

# Расписание №2 (пример)
# YANDEX_FILE_PATH_2="/Другой/Путь/schedule2.xlsx"
# FILE_NAME_2="second_building"

# --- Настройки отображения и кэша (опционально) ---
LOGO_FILE_PATH="img/logo.png"     # Путь к логотипу внутри папки static/
CACHE_DURATION=900                # Время жизни кэша в секундах (15 минут)
CAROUSEL_INTERVAL=8               # Интервал смены слайдов в ландшафтном режиме (секунды)
SHOW_BEFORE_START_MIN=60          # За сколько минут до начала показывать расписание/консультации
SHOW_AFTER_END_MIN=30             # Сколько минут после окончания показывать расписание/консультации
REGION_TIMEDELTA=7                # Часовой пояс региона (например, +7 часов от GMT)
```

#### 5. Запуск приложения

*   **Для разработки:**
    ```bash
    python run.py
    ```
    Приложение будет доступно по адресу `http://127.0.0.1:5000`.

*   **Для production:**
    Рекомендуется использовать Gunicorn или другой WSGI-сервер.
    ```bash
    gunicorn --workers 3 --bind 0.0.0.0:8000 run:app
    ```

## 🧠 Архитектурный обзор

Приложение построено на принципе разделения ответственности.
1.  **`YandexDiskClient`** отвечает только за скачивание и верификацию файла.
2.  **`CacheManager`** является центральным элементом. Он оркестрирует процесс обновления данных: вызывает клиент для скачивания, передает файл парсерам и сохраняет результат в JSON-кэш. При обычных запросах он мгновенно отдает данные из кэша.
3.  **Парсеры** (`parsers/*`) отвечают за самую сложную часть — преобразование "сырых" данных из разных форматов Excel в унифицированные Python-объекты (дата-классы). Логика разделена на:
    *   Основной парсер (`schedule_parser`, `consultation_parser`), извлекающий "сырые" уроки.
    *   "Строители" (`_portrait_builder`, `_landscape_builder`), которые из "сырых" уроков собирают сложные структуры для каждого режима отображения.
4.  **Flask Routes** (`routes.py`, `api_routes.py`) получают данные из кэша, вызывают сервисы-фильтры (`view_filter`) и передают готовые данные либо в HTML-шаблон, либо в виде JSON ответа.

## 🔮 Будущие доработки

*   **Интеграция с Telegram-ботом:** Добавление административного бота для принудительного обновления кэша, перезапуска приложения и получения уведомлений об ошибках.
*   **Улучшение `data_validator`:** Расширение логики валидации данных для более детальной проверки корректности содержимого Excel-файлов.
*   **Декомпозиция `main.js`:** Разбиение основного JS-файла на модули для улучшения читаемости и поддержки.